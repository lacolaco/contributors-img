{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ],
    "source": "functions"
  },
  "hosting": {
    "predeploy": [
      "npm run build:prod"
    ],
    "public": "dist/puppeteer-screenshot-test",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "/contributors",
        "function": "createContributorsImage"
      },
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
